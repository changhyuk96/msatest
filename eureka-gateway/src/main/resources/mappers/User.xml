<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="src.test.gate.user.userDAO">
    <select id="getUser" resultType="src.test.gate.user.userVO" parameterType="string">
        SELECT * from M_USER_table
		WHERE U_NAME = #{U_NAME}
    </select>
</mapper>